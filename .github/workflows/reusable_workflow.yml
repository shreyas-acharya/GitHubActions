name: Reusable Workflow
on:
  push:
    branches:
      - reusable_workflow
jobs:
  Startup:
    runs-on: ubuntu-latest
    steps:
    - name: Get Code
      uses: actions/checkout@v3
    - name: Run Startup
      uses: ./github/startup.yml
      with:
        username: "TEST"
  CheckOutput:
    needs: Startup
    runs-on: ubuntu-latest
    steps:
      - name: Print output
        run: echo '${{ needs.Startup.outputs.time }}'
